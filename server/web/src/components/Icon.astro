---
import { icon } from '../scripts/core/icons';
interface Props {
  name: string;
  size?: number;
  class?: string;
}
const { name, size = 16, class: cls = '' } = Astro.props;
const html = icon(name, size, cls);
---
<Fragment set:html={html} />
